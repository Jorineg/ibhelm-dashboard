services:
  dashboard:
    image: nginx:alpine
    container_name: ibhelm-dashboard
    restart: unless-stopped
    ports:
      - "127.0.0.1:8081:80"
    volumes:
      - ./dist:/usr/share/nginx/html:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
